#命令書: 
あなたは、一流のフルスタックエンジニアです。 
以下の制約条件と質問をもとに、 最高の回答を出力してください。 
回答に時間がかかってもいいので、GPTさん自身で回答に間違いがないか、より良いものにできないかセルフチェックを行ってから回答をいただきたいです。 

#制約条件（Remixアプリケーション）:
・環境はM1Macを想定している 
・アプリケーションの名前は kenken-poke-remix とする
・javascript/jsxではなく、typescript/tsxを用いる
・レイヤードアーキテクチャを採用し、ドメインロジックレイヤー、データアクセスレイヤーなどを適切に分割する
・di、もしくはdiコンテナを利用し、テスタビリティのある設計にすること
・ESLint/prettier/Vitestなどを用いて、きれいなソースコードを作成できるようにする
・ドメインは kenken-mattermost.com を利用する
・https://pokeapi.co （pokeapi）を利用して、図鑑表示などを行う
・以下のような画面を作成する
  - カントー地方/ジョウト地方… などのポケモン一覧画面
    + 各地方のタブを設定し、画面遷移無しで各地方のポケモンを表示できるようにする
    + ポケモンの写真、図鑑No、説明などを表示する、各セルの右上にはお気に入りボタンを設定する
  - ポケモン一覧画面から各セルをタップすると表示される詳細画面
    + 可能な限り詳細に各ポケモンの詳細情報を表示する
    + 画面の右上にはお気に入りボタンを設定する
  - お気に入りポケモン一覧画面
    + お気に入りにしたポケモン一覧セルを表示する
    + セルの表示はポケモン一覧画面と同様にする
    + 各セルをタップすると詳細画面に遷移する
・ポケモンのお気に入りは後述するGCPインフラのCloudSpannerに保存するものとし、保存/削除はCloudSpannerと連携できるようにうまく実装すること
・角丸表示など、マテリアルデザインに沿ったモダンでオシャレな画面を作成すること
・スタイリングにはTailwind CSSを使うこと
・レスポンシブデザインに対応すること
・Web標準に対応すること
・useEffectはなるべく使わない
・ユーザー登録は行わないため、cookie/session等を上手く使ってユーザーを識別する
・Remix公式が提唱するベストプラクティスを適用し、拡張性やセキュリティに配慮したRemixアプリケーションを構築する
・なるべく最新の情報を参照してソースコード等を作成すること
・GCPインフラ上のGKEで動かすことを考えて、Dockerを用いて作成するものとする
  - Dockerfile や、必要であれば docker-composeを作成する
・クラスコメントは必ず書くこと
・メソッドコメントも、メソッドの処理が複雑そうな部分には書くこと
・その他、処理が分かりづらそうな部分にはコメント文で補足を入れること

#制約条件（インフラ）: 
・環境はM1Macを想定している 
・GCPのプロジェクトはこれから作成する 
・インフラの環境構築にTerraformを利用したい、Terraform はインストール済みである
・ドメインは kenken-mattermost.com を、プロジェクトIDは xxxx-yyyy とする
・現在無料期間中だが、無料期間が終わってもなるべく費用を抑えて運用できるように諸々の設定を行う
  - リージョンはus-central1を指定
・GCPの最小構成は以下を想定しているが、必要なものがあれば追加する
1. データベース
  - Cloud Spanner
2. アプリケーションサーバー
  - GKE Autopilot
3. ストレージ
  - Cloud Storage
    + tfstateの管理に利用予定
4. ingress
  - Cloud Loadbalancerを利用予定だが、難しそうであればnginxを利用する
5.vpc
  - CloudVPC
    + GKE、Spannerなどはapp_subnet（プライベートサブネット）で動かすものとし、インターネットから直接アクセスできないようにする
・ディレクトリ構造は以下のようにするが、必要なものがあれば追加する
Kenken-poke-gcp/
├── main.tf                # Terraformエントリーポイント
├── variables.tf           # 変数定義
├── outputs.tf             # 出力定義
├── providers.tf           # GCPプロバイダー設定
├── terraform.tfvars   # 入力値の設定 
├── modules/               # 各リソースのモジュール化
│   ├── gke/
│   │   ├── main.tf        # Kubernetesクラスター定義
│   │   ├── secrets.tf. # kubernetes の yamlで用いるシークレットの管理
│   │   ├── variables.tf   # クラスター用変数
│   │   ├── outputs.tf     # クラスター用出力
│   ├── db/
│   │   ├── main.tf        # Cloud Spanner定義
│   │   ├── variables.tf   # SQL用変数
│   │   ├── outputs.tf     # SQL用出力
│   ├── storage/
│   │   ├── main.tf        # Cloud Storage定義
│   │   ├── variables.tf   # ストレージ用変数
│   │   ├── outputs.tf     # ストレージ用出力
│   ├── vpc/
│   │   ├── main.tf        # CloudVPC定義
│   │   ├── outputs.tf     # vpc用出力
│   │   ├── variables.tf   # vpc用変数
│   │   ├── subnets.tf   # サブネット定義
└── k8s                      # Kubernetesマニフェスト
    ├── service.yaml        # Mattermostデプロイメント
    ├── deployment.yaml      # Mattermostサービス
    ├── cert-manager/
     │   ├── letsencrypt-issuer.yaml  # Let's Encryptの設定など
     │   ├── secrets.yaml. # cert-managerが動作するネームスペースの設定など
    ├── app/
     │   ├── deployment.yaml # Podのデプロイと管理を行うコントローラー、レプリカ数やローリングアップデートの設定を管理、など
     │   ├── service.yaml # クラスタ内のPodに対する永続的なネットワークアクセスを提供、Podは動的に作成・削除されるため、Serviceがその抽象化レイヤーとして機能する予定
    ├── db/ # Spannerに接続するために必要であれば作成すること、ただし私はSpannerの利用方法に関して無知であるため、本当にこのディレクトリが必要かは判断できていない
     │   ├── deployment.yaml # データベースのインスタンス数、Podを識別するためのラベル、接続用のシークレットなど
     │   ├── service.yaml 
・k8sのデプロイにはkubectl を用いる想定だが、iacで管理できる方法があればそれを採用する
・tfファイルで設定するシークレットはtfvarsで指定、kubernetesのyamlで設定するシークレットの管理はgke/secrets.tfで指定するものとし、原則コミットしないようにする
・GCP公式が提唱するベストプラクティスを適用し、可用性やセキュリティに配慮したインフラ基盤を構築する
  - その他、modules/配下やk8s/配下に不足しているファイルやディレクトリ、もしくは追加したほうがいいものがあれば追加する
・なるべく最新の情報を参照してソースコード等を作成すること
・処理が分かりづらそうな部分にはコメント文で補足を入れること
・その他、GCPでサービスを有効化するためのコマンドやIAMの作成が必要であれば記載する

#質問: 
・制約条件を基に、まずはRemixアプリケーションのソースコード等を教えて下さい
  - それが終わったら次の回答でインフラのソースコードを教えて下さい
・主要ファイルだけでなく、全ファイルの詳細を教えて下さい
・全ファイルの作成コマンドを教えて下さい
・GitHub Actions を利用して CI/CD パイプラインを構築する前提とする
  - Remixアプリケーション、GCPインフラ基盤で何かしら必要な設定があればパイプライン構築についても設定する
